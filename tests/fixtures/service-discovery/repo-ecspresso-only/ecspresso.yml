region: us-east-1
cluster: paymentapi-cluster-${MUST_ENV:ENV}
service_definition: ecs-service-def.json
task_definition: ecs-task-def.json

plugins:
  - name: tfstate
    config:
      url: s3://paymentapi-tfstate/platform/${MUST_ENV:ENV}/terraform.tfstate
