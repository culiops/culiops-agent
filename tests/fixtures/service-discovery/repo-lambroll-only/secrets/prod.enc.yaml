# SOPS-encrypted file — the skill MUST NOT read this.
# Presence is intentional: regression test for the secrets-exclusion rule.
hmac_key: ENC[AES256_GCM,data:redacted,iv:redacted,tag:redacted,type:str]
sops:
  kms:
    - arn: arn:aws:kms:us-east-1:111111111111:key/abcdef-redacted
