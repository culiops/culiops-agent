apiVersion: v2
name: widgetapi
description: Widget API service (HTTP + async worker) on EKS.
type: application
version: 0.3.2
appVersion: "2026.04.1"

dependencies:
  - name: datadog
    version: "3.60.1"
    repository: "https://helm.datadoghq.com"
    condition: datadog.enabled
